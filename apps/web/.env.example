# Frontend env (Vite). Never put OPENAI_API_KEY here — keys stay on the backend.

VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_WS_BASE_URL=ws://127.0.0.1:8000/ws/v1
# Optional bearer for authenticated WS in future deploys:
# VITE_WS_TOKEN=
