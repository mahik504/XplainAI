apiVersion: v1
kind: Secret
metadata:
  name: xplainai-secrets
  labels:
    app.kubernetes.io/name: xplainai
    app.kubernetes.io/part-of: xplainai
type: Opaque
stringData:
  # In production, replace with SealedSecrets, ExternalSecrets, or Vault injections
  JWT_SECRET: "replace-with-secure-256-bit-random-secret-key"
  OPENAI_API_KEY: "sk-proj-placeholder-replace-in-target-environment"
  POSTGRES_DSN: "postgresql+asyncpg://navigator:navigator@postgres:5432/navigator"
  REDIS_URL: "redis://redis:6379/0"
