apiVersion: v1
kind: Service
metadata:
  name: xplainai-web
  labels:
    app.kubernetes.io/name: xplainai-web
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: xplainai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: xplainai-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
